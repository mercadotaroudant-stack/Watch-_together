# assets/illustrations/

Place larger illustration/artwork assets here (empty states, onboarding
art, etc.). Already mapped in pubspec.yaml (`assets/illustrations/`),
so anything dropped in this folder is available immediately after
`flutter pub get` — no pubspec edit needed.
