# assets/images/

Place general raster/vector images here (backgrounds, placeholders,
etc.). Already mapped in pubspec.yaml (`assets/images/`), so anything
dropped in this folder is available immediately after `flutter pub get`
— no pubspec edit needed.
