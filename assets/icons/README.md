# assets/icons/

Place custom icon assets here (SVG/PNG icons not covered by
`cupertino_icons`/Material icons). Already mapped in pubspec.yaml
(`assets/icons/`), so anything dropped in this folder is available
immediately after `flutter pub get` — no pubspec edit needed.
