<?xml version="1.0" encoding="utf-8"?>
<!-- Shown for the brief moment between the OS launching this Activity and
     Flutter drawing its first frame — matches AppColors.background
     (#0B0B12) so there's no white flash before this project's own
     SplashScreen widget takes over. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/black" />
</layer-list>
